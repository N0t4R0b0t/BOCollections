# Feature specs

One file per feature, written by `/pitch` (see `.claude/commands/pitch.md`) during Stage 1 before
any planning or implementation starts. Naming: `<kebab-case-feature-name>.md`.

Check here and in `STRETCH_GOALS.md` before pitching something new — it may already be specced.
